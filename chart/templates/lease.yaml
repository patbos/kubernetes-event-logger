apiVersion: coordination.k8s.io/v1
kind: Lease
metadata:
  name: {{ include "kubernetes-event-logger.leaseName" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "kubernetes-event-logger.labels" . | nindent 4 }}
