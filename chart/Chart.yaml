apiVersion: v2
name: kubernetes-event-logger
description: A lightweight Kubernetes event logger that watches cluster events and outputs them as JSON to stdout.
type: application
# Chart version and appVersion are bumped with each release.
# The versions below are examples; actual values are managed by release automation.
version: 0.2.2
appVersion: "0.2.2"
kubeVersion: ">=1.25-0"
keywords:
  - kubernetes
  - events
  - logging
icon: https://raw.githubusercontent.com/patbos/kubernetes-event-logger/main/images/icon.png
home: https://github.com/patbos/kubernetes-event-logger
sources:
  - https://github.com/patbos/kubernetes-event-logger
maintainers:
  - name: patbos
